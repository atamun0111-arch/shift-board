export const metadata = {
  title: "3班現場割",
  description: "3班 現場割・シフト管理システム",
};

export default function RootLayout({ children }) {
  return (
    <html lang="ja">
      <body>{children}</body>
    </html>
  );
}
